#include <stdio.h>


/**
 * main- Program prints a reversed alphabet.
 * Returns: Always 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'z'; i >= 'a'; i--)
		putchar(i);
	putchar('\n');
	return (0);
}
